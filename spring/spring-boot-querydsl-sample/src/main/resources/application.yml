spring:
  datasource:
    url: jdbc:h2:mem:testdb;LOCK_TIMEOUT=15000
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc.lob.non_contextual_creation: true
#        format_sql: true
logging:
  level:
    root: INFO
    org.springframework.web: ERROR
    org.hibernate: INFO
#    org.hibernate.orm.jdbc.bind: TRACE
#    org.springframework.transaction: DEBUG
#    org.springframework.orm.jpa: DEBUG